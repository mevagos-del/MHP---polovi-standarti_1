This folder is reserved for a future Supabase-backed repository implementation.

The UI currently talks to repository/service functions that use localStorage.
Future migration should replace repository implementations while preserving
domain types and component contracts.
